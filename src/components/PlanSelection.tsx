"use client";import{useMemo,useState}from"react";type Plan="core"|"pro";type Interval="month"|"year";const amounts={core:{month:39,year:390},pro:{month:79,year:790}} as const;export default function PlanSelection({cancelled=false,locked=false,status=null}:{cancelled?:boolean;locked?:boolean;status?:string|null}){const[interval,setInterval]=useState<Interval>("month"),[busy,setBusy]=useState<Plan|null>(null);const firstCharge=useMemo(()=>new Date(Date.now()+7*86400000).toLocaleDateString("en-US",{month:"long",day:"numeric",year:"numeric"}),[]);async function checkout(plan:Plan){setBusy(plan);try{const r=await fetch("/api/billing/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({plan,interval})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error||"Unable to start checkout.");if(d?.url)location.href=d.url;else throw new Error("Stripe did not return a checkout link.")}catch(e){alert(e instanceof Error?e.message:"Unable to start checkout.");setBusy(null)}}return <div className="mx-auto max-w-5xl px-5 py-10 sm:px-8 sm:py-14"><div className="mx-auto max-w-2xl text-center"><p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-600">ClearCFO plans</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">{locked ? (status == null ? "Choose a plan to continue." : status === "canceled" ? "Your subscription has ended." : "Your trial has ended.") : "Choose your plan before building your briefing."}</h1><p className="mt-4 text-base leading-7 text-slate-600">{locked ? "Your data is safe — subscribe to continue." : "Start with a 7-day free trial. Your card is collected at checkout, and you won&apos;t be charged until the trial ends."}</p>{cancelled?<p className="mt-4 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-medium text-slate-600">You left checkout before finishing — your trial hasn&apos;t started.</p>:null}<div className="mt-6 inline-flex rounded-xl border border-slate-200 bg-white p-1 shadow-sm"><button type="button" onClick={()=>setInterval("month")} className={`rounded-lg px-5 py-2 text-sm font-semibold ${interval==="month"?"bg-blue-600 text-white":"text-slate-600"}`}>Monthly</button><button type="button" onClick={()=>setInterval("year")} className={`rounded-lg px-5 py-2 text-sm font-semibold ${interval==="year"?"bg-blue-600 text-white":"text-slate-600"}`}>Annual <span className="ml-2 text-xs">2 months free</span></button></div></div><div className="mx-auto mt-10 grid max-w-4xl gap-5 md:grid-cols-2">{(["core","pro"] as Plan[]).map(plan=>{const p=amounts[plan][interval],features=plan==="core"?["CFO briefing in plain English","Revenue & margin analysis","Expense analysis","Cash & working capital","Customer & vendor movers","AI recommendations with dollar impact"]:["Everything in Core","Weekly CFO report by email","Automatic syncing, always current","Proactive alerts before issues grow","Custom alerts on your own thresholds","Up to 5 businesses in one login"];return <section key={plan} className={`relative flex flex-col rounded-2xl bg-white p-7 shadow-sm ${plan==="pro"?"border-2 border-blue-600 shadow-lg shadow-blue-600/10":"border border-slate-200"}`}>{plan==="pro"?<div className="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-blue-600 px-3 py-1 text-xs font-bold text-white">MOST POPULAR</div>:null}<div className="flex items-baseline justify-between gap-4"><h2 className="text-xl font-bold text-slate-900">ClearCFO {plan==="core"?"Core":"Pro"}</h2><p className="text-3xl font-extrabold text-slate-900">${p}<span className="text-base font-medium text-slate-500">/{interval==="month"?"mo":"yr"}</span></p></div><ul className="mt-7 space-y-3 text-sm text-slate-600">{features.map(f=><li key={f} className="flex gap-2"><span className="font-bold text-emerald-600">✓</span><span>{f}</span></li>)}</ul><div className="mt-7 rounded-xl border border-blue-100 bg-blue-50/60 p-4 text-sm text-slate-700"><p className="font-semibold">7-day free trial · Card required · First charge {firstCharge} — ${p}{interval==="year"?"/year":"/month"}.</p><p className="mt-2 text-xs leading-5 text-slate-500">If you don&apos;t cancel before the trial ends, billing starts automatically for the selected plan and interval.</p></div><button type="button" onClick={()=>void checkout(plan)} disabled={busy!==null} className={`mt-6 rounded-xl py-3 text-center text-sm font-semibold ${plan==="pro"?"bg-blue-600 text-white hover:bg-blue-700":"border border-slate-300 bg-white text-slate-700 hover:bg-slate-50"} disabled:opacity-60`}>{busy===plan?"Opening checkout…":"Start 7-day trial"}</button></section>})}</div></div>}
