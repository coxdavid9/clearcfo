import {NextResponse} from "next/server";import{getSubscription,requireBillingUser,hasAccess}from "../../../../lib/billing/entitlements";export const runtime="nodejs";export async function GET(){try{const id=await requireBillingUser(),s=await getSubscription(id);return NextResponse.json({plan:s?.plan??null,interval:s?.interval??null,status:s?.status??null,trialEndsAt:s?.trial_ends_at??null,currentPeriodEnd:s?.current_period_end??null,hasAccess:hasAccess(s)})}catch(e){const m=e instanceof Error?e.message:"Unable to read billing status.";return NextResponse.json({error:m},{status:m==="Unauthorized"?401:500})}}