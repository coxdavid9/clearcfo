import{NextResponse}from"next/server";import{stripeGet,upsertSubscription,mapStatus}from "../../../../lib/billing/stripe";export const runtime="nodejs";export async function GET(request:Request){const site=(process.env.NEXT_PUBLIC_SITE_URL||"https://theclearcfo.com").replace(/\/$/,""),sessionId=new URL(request.url).searchParams.get("session_id");if(!sessionId)return NextResponse.redirect(site+"/customer/briefing");try{const s=await stripeGet("checkout/sessions/"+encodeURIComponent(sessionId)+"?expand[]=subscription"),sub=s.subscription,userId=s.metadata?.supabase_user_id||sub?.metadata?.supabase_user_id;if(userId&&sub)await upsertSubscription({user_id:userId,stripe_customer_id:s.customer,stripe_subscription_id:sub.id,plan:sub.metadata?.plan||s.metadata?.plan,interval:sub.metadata?.interval||s.metadata?.interval,status:mapStatus(sub.status),trial_ends_at:sub.trial_end?new Date(sub.trial_end*1000).toISOString():null,current_period_end:sub.current_period_end?new Date(sub.current_period_end*1000).toISOString():null,cancel_at_period_end:!!sub.cancel_at_period_end,is_comp:false});return NextResponse.redirect(site+"/customer/briefing")}catch(e){console.error("[Billing] return",e);return NextResponse.redirect(site+"/customer/briefing?billing=error")}}